# Migration `20200831001354-dasd`

This migration has been generated by Ryan Lee at 8/30/2020, 8:13:54 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" text   NOT NULL ,
"email" text   NOT NULL ,
"password" text   NOT NULL ,
"isActive" boolean   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."FeedSource" (
"id" text   NOT NULL ,
"url" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."FeedItem" (
"id" text   NOT NULL ,
"title" text   NOT NULL ,
"link" text   NOT NULL ,
"date" timestamp(3)   NOT NULL ,
"content" text   ,
"author" text   ,
"image" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."_FeedItemToFeedSource" (
"A" text   NOT NULL ,
"B" text   NOT NULL 
)

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")

CREATE UNIQUE INDEX "_FeedItemToFeedSource_AB_unique" ON "public"."_FeedItemToFeedSource"("A", "B")

CREATE INDEX "_FeedItemToFeedSource_B_index" ON "public"."_FeedItemToFeedSource"("B")

ALTER TABLE "public"."_FeedItemToFeedSource" ADD FOREIGN KEY ("A")REFERENCES "public"."FeedItem"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_FeedItemToFeedSource" ADD FOREIGN KEY ("B")REFERENCES "public"."FeedSource"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200831001354-dasd
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id       String  @id
+  email    String  @unique
+  password String
+  isActive Boolean
+}
+
+model FeedSource {
+  id       String     @id
+  url      String
+  FeedItem FeedItem[]
+}
+
+model FeedItem {
+  id          String       @id
+  feedSources FeedSource[]
+  title       String
+  link        String
+  date        DateTime
+  content     String?
+  author      String?
+  image       String?
+}
```


